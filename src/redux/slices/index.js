//user slice
export { userReducer, authenticateUser, logout } from "./userSlice";

//product slice
export {
  productReducer,
  setSelecdetProduct,
  saveProduct,
  fetchHomePageProducts,
} from "./productSlice";
